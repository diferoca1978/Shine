---
import Logo from "@/assets/images/LogoShineWhite(240x110px).svg";
import { navBarLinksConst } from "@/utils/navigation";
import { COMPANY_INFO } from "@/config/seo";
import InstagramIcon from "@/assets/icons/instagram.svg";
import LinkedInIcon from "@/assets/icons/linkedin.svg";
import TikTokIcon from "@/assets/icons/tiktok.svg";
---

<footer
  class="w-full bg-softWhite/95 backdrop-blur-sm relative border-t border-smokyBlack/10 mt-20"
>
  <!-- Main Footer Content -->
  <div class="max-w-7xl mx-auto px-4 py-4 lg:py-8">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 lg:gap-8">
      <!-- Company Section -->
      <div class="lg:col-span-2">
        <div class="mb-3">
          <a href="/" aria-label="Home" class="inline-block">
            <Logo class="h-16 w-auto object-contain" />
          </a>
        </div>
        <h3 class="text-lg font-playfair font-medium text-smokyBlack mb-3">
          Haz que tu luz brille y atraiga a quienes necesitan tu talento
        </h3>
        <p
          class="prose text-smokyBlack/80 font-openSans leading-relaxed mb-6 max-w-md"
        >
          Transformamos tu presencia digital con estrategia, propósito y
          autenticidad. Diseño web, marketing digital y contenido que refleja tu
          verdadera esencia.
        </p>

        <!-- Social Media -->
        <div class="flex space-x-4">
          <a
            href={COMPANY_INFO.socialMedia.instagram}
            target="_blank"
            rel="noopener noreferrer"
            class="text-smokyBlack/70 hover:text-accentGold transition-colors duration-300"
            aria-label="Instagram"
          >
            <InstagramIcon fill="currentColor" />
          </a>
          <a
            href={COMPANY_INFO.socialMedia.linkedin}
            target="_blank"
            rel="noopener noreferrer"
            class="text-smokyBlack/70 hover:text-accentGold transition-colors duration-300"
            aria-label="LinkedIn"
          >
            <LinkedInIcon />
          </a>
          <a
            href={COMPANY_INFO.socialMedia.tiktok}
            target="_blank"
            rel="noopener noreferrer"
            class="text-smokyBlack/70 hover:text-accentGold transition-colors duration-300"
            aria-label="TikTok"
          >
            <TikTokIcon />
          </a>
        </div>
      </div>

      <!-- Navigation Links -->
      <div>
        <h4 class="text-lg font-playfair font-medium text-smokyBlack mb-3">
          Navegación
        </h4>
        <ul class="space-y-3">
          {
            navBarLinksConst.map((item) => (
              <li>
                <a
                  href={item.path}
                  class="text-smokyBlack/70 hover:text-accentGold font-openSans transition-colors duration-300"
                >
                  {item.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <!-- Contact Information -->
    <div class="mt-6 pt-4 border-t border-smokyBlack/10">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div>
          <h5 class="font-playfair font-medium text-smokyBlack mb-2">
            Ubicación
          </h5>
          <p class="text-smokyBlack/70 font-openSans text-sm">
            {COMPANY_INFO.address.city}, {COMPANY_INFO.address.country}
          </p>
        </div>
        <div>
          <h5 class="font-playfair font-medium text-smokyBlack mb-2">
            Contacto
          </h5>
          <p class="text-smokyBlack/70 font-openSans text-sm">
            <a
              href={`mailto:${COMPANY_INFO.email}`}
              class="hover:text-accentGold transition-colors duration-300"
            >
              {COMPANY_INFO.email}
            </a>
          </p>
          <p class="text-smokyBlack/70 font-openSans text-sm">
            <a
              href={`tel:${COMPANY_INFO.phone}`}
              class="hover:text-accentGold transition-colors duration-300"
            >
              {COMPANY_INFO.phone}
            </a>
          </p>
        </div>
        <div class="md:text-right">
          <button
            id="back-to-top"
            class="inline-flex items-center px-4 py-2 text-sm font-openSans font-medium text-smokyBlack bg-softBeige border border-salviaGreen rounded-xl hover:bg-salviaGreen hover:text-softWhite transition-all duration-300 hover:scale-105"
            aria-label="Volver arriba"
          >
            <svg
              class="w-4 h-4 mr-2"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 10l7-7m0 0l7 7m-7-7v18"></path>
            </svg>
            Volver arriba
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="border-t border-smokyBlack/10 bg-smokyBlack/5">
    <div class="max-w-7xl mx-auto px-4 py-1">
      <div
        class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0"
      >
        <div
          class="text-smokyBlack/60 font-openSans text-sm text-center md:text-left"
        >
          © {new Date().getFullYear()}
          {COMPANY_INFO.name}. Todos los derechos reservados.
        </div>
        <div class="flex space-x-6 text-sm font-openSans">
          <a
            href="/politica-privacidad"
            class="text-smokyBlack/60 hover:text-accentGold transition-colors duration-300"
          >
            Política de Privacidad
          </a>
          <a
            href="/terminos-condiciones"
            class="text-smokyBlack/60 hover:text-accentGold transition-colors duration-300"
          >
            Términos y Condiciones
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  // Back to top functionality
  const backToTopButton = document.getElementById("back-to-top");

  if (backToTopButton) {
    backToTopButton.addEventListener("click", () => {
      window.scrollTo({
        top: 0,
        behavior: "smooth",
      });
    });
  }
</script>
