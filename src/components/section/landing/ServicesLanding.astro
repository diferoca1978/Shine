---
import { services } from "@/config/services";
import Star from "@/assets/icons/star40x40black.svg";
import ServiceCard from "@/components/ui/ServiceCard.astro";
import { Image } from "astro:assets";
import StarIcon from "@/assets/icons/star40x40.svg";
import Button from "@/components/ui/button.astro";
---

<section class="services-wrapper w-full mx-auto">
  <div class="services-container max-w-7xl mx-auto px-4 py-8 md:py-12 lg:py-20">
    <div class="flex flex-col items-center justify-center">
      <!-- Title -->
      <div class="flex flex-col items-center justify-center gap-4 mb-16">
        <div class="btn flex items-center">
          <Button
            variant="optional2"
            href="/servicios"
            className="py-1 px-4 rounded-4xl"
            animation={true}
          >
            Servicios
            <Star class="star w-9 h-9" />
          </Button>
        </div>
        <h1
          class="main-title text-smokyBlack font-playfair font-medium text-4xl md:text-5xl lg:text-6xl"
        >
          ¿Cómo te ayudamos a crecer?
        </h1>
      </div>
      <!-- End Title -->

      <!-- Services -->

      <div
        class="services-grid grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3 mb-16"
      >
        {
          services.map((service) => (
            <ServiceCard
              slug={service.slug}
              image={service.image}
              alt={service.alt}
              title={service.title}
              description={service.subtitle || ""}
              tags={service.tags || []}
            />
          ))
        }
      </div>
    </div>
  </div>
</section>

<script>
  import { servicesAltAnimation } from "@/utils/scripts/perPageAnimations/home/servicesAltAnimation.js";

  document.removeEventListener("DOMContentLoaded", servicesAltAnimation);
  document.addEventListener("DOMContentLoaded", servicesAltAnimation);
</script>
